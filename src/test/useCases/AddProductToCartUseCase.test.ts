import { before, describe, it } from "node:test";

describe("AddProductToCartUseCase", () => {

    it("deve adicionar um novo produto ao carrinho caso ele não exista", async () => {

    });

    it("deve atualizar a quantidade se o produto já estiver no carrinho", async () => {

    });
    
    it("deve lançar erro se o carrinho não for encontrado", async () => {

    });

    it("deve lançar erro se o produto não for encontrado", async () => {

    });
});